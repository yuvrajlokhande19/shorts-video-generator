Flask
python-dotenv
edge-tts
pydub
numpy
requests

# Optional (only needed if you add a GEMINI_API_KEY in .env):
#   pip install google-generativeai
# The app automatically falls back to a built-in template script generator
# when no key is configured, so this is not required to run.
